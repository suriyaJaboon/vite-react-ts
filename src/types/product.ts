export type Product = {
    id: string
    uuid: string
    license_id: string
    secret: string
    ip_addr: string
    server_name: string
    version: string
    organization_name: string
    full_name: string
    position: string
    email: string
    tel: string
    started_at: string
    expired_at: string
    timestamp: string
    created_at: string
    approval: boolean
}